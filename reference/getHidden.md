# Get or set hidden globally accessible elements

Get or set hidden globally accessible elements

## Usage

``` r
getHidden()

setHidden(val)
```

## Arguments

- val:

  Value to attribute

## Value

Getters return hidden globally accessible data, whereas setters return
`NULL` as they are only used to modify the state of hidden elements
