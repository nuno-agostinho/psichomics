# Return an user interface depending on the status of the FireBrowse API

If the API is working, it'll be loaded. Else, a message will appear
warning the user that the API is down and that will let check again if
the API is back online.

## Usage

``` r
checkFirebrowse(ns)
```

## Arguments

- ns:

  Namespace function

## Value

HTML elements
