# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
r: bioc-devel
sudo: false
cache: packages
bioc_packages:
  - GenomeInfoDbData
  - DelayedArray
after_success:
  - Rscript -e 'covr::codecov()'