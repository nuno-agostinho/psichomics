# Add clusters to `highchart` object

Clusters are added as coloured polygons.

## Usage

``` r
plotClusters(hc, data, clustering)
```

## Arguments

- hc:

  `highchart` object

- data:

  Data frame

- clustering:

  Character: group of each sample

## Value

`highcharter` object
