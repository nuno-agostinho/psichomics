# Render group interface

Render group interface

## Usage

``` r
renderGroupInterface(ns, multiFisherTests = TRUE)
```

## Arguments

- ns:

  Namespace function

- multiFisherTests:

  Boolean: allow to perform multiple Fisher exact test between groups

## Value

HTML elements
